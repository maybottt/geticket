services:
  db:
    image: postgres:17
    container_name: geticket_db
    restart: always
    ports: 
      - "5433:5432"
    environment:
      POSTGRES_DB: ${DATABASE_NAME}
      POSTGRES_USER: ${DATABASE_USERNAME}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    env_file:
      - .env

  web:
    build: ./backend
    container_name: geticket_app
    command: >
      sh -c "python manage.py migrate &&
             gunicorn --bind 0.0.0.0:8000 --workers 3 config.wsgi:application"
    volumes:
      - ./backend/app:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
    env_file:
      - .env

  frontend: 
    build: ./frontend 
    container_name: geticket_frontend 
    volumes: 
      - ./frontend:/app
      - /app/node_modules 
    ports: 
      - "5173:5173"
    depends_on: 
      - web 

volumes:
  postgres_data:
